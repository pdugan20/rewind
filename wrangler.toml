name = "rewind"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]
workers_dev = true

# Custom domain
routes = [
  { pattern = "api.rewind.rest", custom_domain = true }
]

# D1 Database
[[d1_databases]]
binding = "DB"
database_name = "rewind-db"
database_id = "35a4edf8-0d4f-4cbe-9a6e-6f1525716774"
migrations_dir = "migrations"

# R2 Bucket
[[r2_buckets]]
binding = "IMAGES"
bucket_name = "rewind-images"

# Cron Triggers
[triggers]
crons = [
  "*/15 * * * *",   # Every 15 min: Last.fm scrobble sync
  "0 3 * * *",      # Daily 3:00 AM: Last.fm top lists + stats
  "15 3 * * *",     # Daily 3:15 AM: Strava activities
  "30 3 * * *",     # Daily 3:30 AM: Plex library scan
  "45 3 * * SUN",   # Sunday 3:45 AM: Discogs + Trakt collections
  "0 */6 * * *",    # Every 6 hours: Letterboxd RSS sync
]

# Cloudflare Images (for thumbhash/color extraction)
[images]
binding = "IMAGE_TRANSFORMS"

[vars]
ALLOWED_ORIGINS = "patdugan.me,localhost:3000"
